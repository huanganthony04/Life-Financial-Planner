const BACKEND_URL = import.meta.env.VITE_BACKEND_URL
const Home = () => {

    return (
        <div id="home_page" className="page">
            <div>
                <h1>Dashboard</h1>
            </div>
        </div>
        )
    }

export default Home